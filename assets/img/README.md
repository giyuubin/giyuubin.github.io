# Images

Drop a square profile photo here as `profile.jpg` (≥320×320 recommended; it is
displayed at 160px, 128px on mobile).

Until that file exists the hero falls back to a circle with the initials from
`initials` in `assets/data/profile.json`, so a missing photo never shows a
broken image.
